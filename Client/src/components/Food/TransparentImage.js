import React from "react";

export default function TransparentImage({name}) {
  return (
    <section className="common_section">
      <div className="innerTransparentCon">
        <h2 className="text-white">{name}</h2>
      </div>
    </section>
  );
}
